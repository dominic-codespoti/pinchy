# Test Command

Runs the test suite.

## Usage

```bash
kilo test
```

## What it does

- `cargo test --no-default-features --lib`

## Integration tests

Tests are in `tests/` directory (21 integration test files).
